//Require the express module
const express = require("express");

//Create a router
const router = express.Router();

//GET /route - returns JSON object with message key assigned to definition of route in API

//GET /controller - returns JSON object with message key assigned to definition of controller in API

//GET /root - returns name of entrypoint root file for this app

module.exports = router;
